from distutils.core import setup

setup(name='pychidg',
      version='${ChiDG_VERSION_MAJOR}.${ChiDG_VERSION_MINOR}',
      package_dir={ '' : '${CMAKE_CURRENT_BINARY_DIR}' },
      package_data={ 'pychidg' : ['_chidg_core.so']},
      packages=['pychidg'],
      )

